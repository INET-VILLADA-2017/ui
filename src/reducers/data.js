import {

} from '../constants/actionTypes'

const initialState = {
    parameter: null,
    samples: []
}

export default function data(state = initialState, action = {}) {
    switch (action.type) {

    }
    return state
}
