import React from 'react'
import './styles.css'

const NotFound = () => (
    <div className={'NotFound'}>
        <span className={'NotFound_title'}>Perdón pero...</span>
        <span className={'NotFound_sub'}>Parece que la página a la que querés acceder no existe :(</span>
    </div>
)

export default NotFound
